# Copy to .env and fill in values
PORT=3001
JWT_SECRET=your-super-secret-key-change-this-in-production
NODE_ENV=production
# DB_PATH defaults to ./backend/data.json — Railway persists the volume
DB_PATH=/data/taskflow.json
